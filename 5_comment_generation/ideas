Suggest a few names that fit the following function in C# language the most:
```C#
    public virtual object foo(LabeledStatement labelStatement, object data)
    {
      stackMap.Push(labelStatement);
      labelStatement.Statement.AcceptVisitor(this, data);
      stackMap.Pop();
      return null;
    }
```

