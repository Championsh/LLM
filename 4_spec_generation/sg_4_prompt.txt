Imagine you are a programmer creating static code analysis tool.
Generate function with prototype int SysReAllocString(BSTR *pbstr, const OLECHAR *psz), relying on an existing function BSTR SysAllocString(const OLECHAR *psz):
BSTR SysAllocString(const OLECHAR *psz) {
    BSTR ptr;
    sf_overwrite(&ptr);
    sf_overwrite(ptr);
    sf_uncontrolled_ptr(ptr);
    sf_set_alloc_possible_null(ptr);
    sf_new(ptr, BSTR_ALLOC_CATEGORY);

    sf_copy_string(ptr, psz);
    return ptr;
};
Consider to generate a lightweight code, i.e. we need only general function behaviour without assignment, we don't need cycles too.
